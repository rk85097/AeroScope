# Release Firmware

This folder contains the latest public firmware binary.

- `firmware.bin` - latest AeroScope firmware
- `SHA256SUMS.txt` - checksum for verification

Use the flashing command from the main README.
